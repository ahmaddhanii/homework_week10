// views/contactView.js
class ContactView {
    renderContacts(contacts) {
      // Implementasikan kode untuk merender daftar kontak kepada pengguna.
    }
    
    renderContact(contact) {
      // Implementasikan kode untuk merender satu kontak kepada pengguna.
    }
  
    // Tambahkan metode untuk menangani interaksi pengguna dan tampilan (misalnya, formulir).
  }
  
  module.exports = ContactView;
  